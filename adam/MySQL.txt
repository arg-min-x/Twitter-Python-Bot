SHOW databases
CREATE DATABASE twitterTest;
USE twitterTest
CREATE TABLE user(screenName VARCHAR(20), name VARCHAR(30), ID BIGINT, description VARCHAR(200), location VARCHAR(50), follower TINYINT(1), following TINYINT(1), friendsCount BIGINT, followersCount BIGINT, favouritesCount BIGINT, defaultProfile TINYINT(1), verified TINYINT(1), followRequestSent TINYINT(1), timezone varchar(30), dateCreated DATETIME, dateCrawled DATETIME);
DESCRIBE user;
SELECT * FROM user;
SELECT * FROM user LIMIT 10;
drop table user;
select * from user where screenName like 'Carmart_13';
SELECT  sum(round(((data_length + index_length) / 1024 / 1024), 2))  as "Size in MB" FROM information_schema.TABLES  WHERE table_schema = "twitterTest";
show warnings;
show processlist;

% look for duplicates
mysql> SELECT a.id
    -> FROM user2 a
    -> INNER JOIN user2 b ON a.id = b.id
    -> WHERE a.id <> b.id;

