CREATE DATABASE twitterTest;
USE twitterTest
CREATE TABLE user(screenName VARCHAR(20), name VARCHAR(30), ID BIGINT, description VARCHAR(200), location VARCHAR(50), friend TINYINT(1), following TINYINT(1), friendsCount BIGINT, followersCount BIGINT, favouritesCount BIGINT, defaultProfile TINYINT(1), verified TINYINT(1), followRequestSent TINYINT(1), timezone varchar(30), dateCreated DATETIME, dateCrawled DATETIME);
DESCRIBE user;
SELECT * FROM user;
SELECT * FROM user LIMIT 10;
drop table user;
select * from user where screenName like 'Carmart_13';
